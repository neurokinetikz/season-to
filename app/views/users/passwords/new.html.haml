%div.span2

%div.span8
  = form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, :id => 'password-form', :class => 'form-horizontal' }) do |f|
    = devise_error_messages!
    %fieldset
      %legend Forgot Your Password?
      %div.control-group
        %label.control-label{:for => 'user_email'} Email
        %div.controls
          %input#user_email.required.email{:type => 'text', :name => 'user[email]', :placeholder => 'Email', :autofocus => 'true'}
          
      %div.control-group
        %div.controls
          %button.btn{:type => 'submit'} Send me reset password instructions
          
      %div.control-group
        %div.controls
          = render "devise/shared/links"
          
%div.span2

= content_for :javascript do
  $("#password-form").validate();
