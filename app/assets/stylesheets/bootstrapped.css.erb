body {
  padding-top:40px;
  font-family: 'Sanchez', serif;
  position: relative;
}

.jumbotron {
  background-image:url(<%=asset_path 'jumbotron.jpg'%>);
  background-position: center center;
  background-repeat: no-repeat;
  background-size:cover;
  height:600px;
}

.jumbotron h1, .jumbotron p {
  background-color:rgba(0,0,0,0.5);
  color:white;
  padding:5px 0px 5px 20px;
}

.section {
  margin-bottom:30px;
}

#jumbo-buttons {
  margin-top:70px;
}

#jumbo-buttons div a {
  margin-top:10px;
}

.footer {
  border-top: 1px solid rgba(0,0,0,0.1);
  margin-top:40px;
  background-color: rgba(0,0,0,0.1);
  font-size: 10px;
}


label.error {
  color: rgba(255,0,0,0.7);
}

.alerts{ margin-top:8px;}


/*@font-face {
  font-family: 'ChunkFiveRegular';
  src: url(<%= asset_path 'Chunkfive-webfont.eot' %>);
  src: url(<%= asset_path 'Chunkfive-webfont.eot?#iefix' %>) format('embedded-opentype'), url(<%= asset_path 'Chunkfive-webfont.woff' %>) format('woff'), url(<%= asset_path 'Chunkfive-webfont.ttf' %>) format('truetype'), url(<%= asset_path 'Chunkfive-webfont.svg#ChunkFiveRegular' %>) format('svg');
  font-weight: normal;
  font-style: normal;
}

body {
  padding-top:60px;
}

nav a {
  font-family: 'Open Sans', sans-serif;
}

a.navbar-brand {
  font-family: 'ChunkFiveRegular', 'Open Sans', sans-serif;
  font-size:32px;
}

#intro { 
    background: url(<%= image_path 'intro.png' %>) 50% 0 fixed; 
    height: auto;  
    margin: 0 auto; 
    width: 100%; 
    position: relative; 
    box-shadow: 0 0 50px rgba(0,0,0,0.8);
    padding: 100px 0;
}
#home { 
    background: url(<%= image_path 'cover.jpg' %>) 50% 0 fixed; 
    height: auto;  
    margin: 0 auto; 
    width: 100%; 
    position: relative; 
    box-shadow: 0 0 50px rgba(0,0,0,0.8);
    padding: 200px 0;
}
#about { 
    background: url(<%= image_path 'about.png' %>) 50% 0 fixed; 
    height: auto;
    margin: 0 auto; 
    width: 100%; 
    position: relative; 
    box-shadow: 0 0 50px rgba(0,0,0,0.8);
    padding: 100px 0;
    color: #fff;
}

h3.panel-title {
  font-family: 'ChunkFiveRegular', 'Open Sans', sans-serif;
  font-weight:normal;
}


.footer { margin-top:30px; }

.btn-default:hover, .btn-default:focus, .btn-default:active, .btn-default.active, .open .dropdown-toggle.btn-default {
color: #fff;
background-color: #aaa;
border-color: #adadad;
font-weight:bold;
}

.alert {
	padding:10px;
}

@media (max-width: 767px) {
	h1 { font-size:24px; }
	.nav-tabs > li > a {font-size:12px;}
}*/